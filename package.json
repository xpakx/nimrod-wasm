{
  "name": "nimrod-wasm",
  "version": "1.0.0",
  "description": "isometric game in wasm",
  "main": "index.js",
  "scripts": {
    "test": "npx jest",
    "build": "npx tsc && npm run wasm && cp -r index.html img/ maps/ styles.css dist",
    "wasm": "make -C wasm OUTPUT_DIR=../dist"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "typescript": "^5.7.3"
  }
}
